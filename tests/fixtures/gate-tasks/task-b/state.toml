# tests/fixtures/gate-tasks/task-b/state.toml — SYNTHETIC fixture: the
# "current"/proposing task in tests/test_gate.py's cross-task tests
# (empty; no assignments yet). Own targets (/mnt/example/target-b) are
# deliberately disjoint from task-a's and task-c's, so tests choose which
# targets the proposed candidate node uses to exercise overlap vs no
# overlap.
schema = 1
assignments = []

[task]
project = "gate-fixtures"
slug = "task-b"
title = "Gate fixture task B (proposing task)"
phase = "intake"
kit_root = "/mnt/example/kit"
targets = ["/mnt/example/target-b"]
journal = "tests/fixtures/gate-tasks/task-b/checkpoint.md"
next_nn = 1
created_at = 2026-01-01T10:00:00+00:00
updated_at = 2026-01-01T10:00:00+00:00
