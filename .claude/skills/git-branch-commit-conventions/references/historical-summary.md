# Resumo histórico — atualidade não verificada

Este conteúdo resume o registro anterior do kit, que declarava consulta em **20/08/2026**. A data e os IDs foram preservados como proveniência histórica; os documentos não foram disponibilizados nem verificados nesta atualização. Não usar este resumo isoladamente para impor regras ou declarar conformidade.

### Convenções registradas anteriormente

O resumo anterior usava `<label>/<XXXX>-<tarefa>_<ação>_<artefato>_<local>`, com `bugfix`, `feature`, `hotfix` ou `improvement`, seção confirmada (exemplo `NADS`), ID sem `#` e descrição minúscula sem acentos, separada por `_`. Exemplo: `feature/NADS-1234_adicionar_endpoint_usuarios_api`. A base citada era uma branch SPRINT vigente; isso não estabelece a base para outros projetos ou tarefas.

O changelog descrito usava `[Unreleased]`, categorias `Adicionado`, `Alterado`, `Corrigido`, `Removido`, `Segurança` e `- Tarefa #<ID>: <descrição>`, na branch da tarefa. O registro vinculava NOTEC-2601181721552 e NOTEC-26032410441464 ao trabalho cotidiano e NOTEC-2511121308520 ao fechamento de release em Developer; essa interpretação histórica não foi revalidada.

A mensagem descrita era `<tipo>: <Descrição no infinitivo> (#<tarefa>)`, corpo numerado opcional e referência `#<tarefa>` final. Tipos registrados: `feat`, `fix`, `refactor`, `style`, `doc` (ou `docs` exigido pelo projeto), `env`, `build`, `test`, `chore`. Sugeria assunto de 50 caracteres quando possível, corpo de 72 por linha, inicial maiúscula e assunto sem pontuação final. Confirmar convenções e hooks reais antes de aplicar esses detalhes.

Identificadores registrados anteriormente: ADR-2410141234520, ADR-2410141520520, ADR-2501060650520, NOTEC-2511121308520, NOTEC-2601181721552 e NOTEC-26032410441464.
